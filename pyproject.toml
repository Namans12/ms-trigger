[project]
name = "ms-trigger"
version = "0.1.0"
description = "Deployment project"
requires-python = ">=3.12"
dependencies = [
    "requests==2.32.4",
    "tzdata==2025.2",
]

[tool.vercel]
entrypoint = "api.releases:handler"
